# RunenECS

RunenECS is a deterministic entity-component-system framework providing worlds,
components, resources, queries, deferred structural commands, and ECS-native
schedule semantics.

The implementation is currently maintained in the Runenwerk repository. External
source transfer is a separate post-conformance operation.
